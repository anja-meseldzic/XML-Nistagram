import { ProfileVerificationRequest } from './profile-verification-request';

describe('ProfileVerificationRequest', () => {
  it('should create an instance', () => {
    expect(new ProfileVerificationRequest()).toBeTruthy();
  });
});
